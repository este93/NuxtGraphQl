query Post($slug: String!){
    post (filter: {slug: {eq: $slug}}) {
	    title
	    description
	    thumbnail{
            url
        }
        video{
        	providerUid
        	url
        }
    }
}